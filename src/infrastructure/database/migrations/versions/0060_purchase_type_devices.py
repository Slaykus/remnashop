"""transactions: a purchase that buys devices, not time

Revision ID: 0060
Revises: 0059
Create Date: 2026-09-21

Докупка устройств проходит тем же платёжным трактом, что и подписка, —
теми же шлюзами, с тем же чеком и теми же уведомлениями. Отличается она
тем, что делает после оплаты: срок и трафик не трогает, поднимает только
потолок устройств. Для этого ей нужен свой вид покупки.

Значение только добавляется. Существующие строки не читаются и не
переписываются, старый код новое значение просто никогда не встретит.
"""

from alembic import op

revision = "0060"
down_revision = "0059"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # Тот же приём, которым в проект добавлялись значения шлюзов и ролей
    # (миграции 0013 и 0018). Postgres разрешает это внутри транзакции,
    # пока новое значение в ней же не используется — а мы только объявляем.
    op.execute("ALTER TYPE purchase_type ADD VALUE IF NOT EXISTS 'DEVICES'")


def downgrade() -> None:
    # Значение из перечисления postgres не выкидывает. Пересоздавать тип
    # ради отката опаснее, чем оставить лишнее имя: на него будут ссылаться
    # уже проведённые платежи.
    pass
